# Source layout

`Forge.Core` contains minimal contracts; `Forge.Web` is the reference host/admin surface; `Forge.AppHost` is local orchestration only. Capability modules should be added as explicit, independently owned boundaries.
