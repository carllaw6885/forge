<Project Sdk="Microsoft.NET.Sdk.Razor">
  <PropertyGroup>
    <!-- pinned like the shell: default base path would follow PackageId (ForgeStack.*) -->
    <StaticWebAssetBasePath>_content/Forge.Identity.Ui.Blazor</StaticWebAssetBasePath>
  </PropertyGroup>
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <!-- ADR 40: the application contract, plus the shell's contribution contract — no stores, no managers -->
    <ProjectReference Include="../Forge.Admin.Blazor/Forge.Admin.Blazor.csproj" />
    <ProjectReference Include="../Forge.Identity/Forge.Identity.csproj" />
  </ItemGroup>
</Project>
