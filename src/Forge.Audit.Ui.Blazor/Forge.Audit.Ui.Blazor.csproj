<Project Sdk="Microsoft.NET.Sdk.Razor">
  <PropertyGroup>
    <!-- pinned like the shell: default base path would follow PackageId (ForgeStack.*) -->
    <StaticWebAssetBasePath>_content/Forge.Audit.Ui.Blazor</StaticWebAssetBasePath>
  </PropertyGroup>
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <!-- ADR 40: the application contract, plus the shell's contribution contract — no stores, no shell -->
    <ProjectReference Include="../Forge.Admin.Abstractions/Forge.Admin.Abstractions.csproj" />
    <ProjectReference Include="../Forge.Auditing/Forge.Auditing.csproj" />
  </ItemGroup>
</Project>
